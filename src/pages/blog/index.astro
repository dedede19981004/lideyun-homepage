---
import Base from '../../layouts/Base.astro';
import { getCollection } from 'astro:content';
import { formatDate } from '../../lib/utils';

const posts = (await getCollection('blog'))
  .filter((p) => !p.data.draft)
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
---
<Base title="博客">
  <div class="container">
    <header class="page-head">
      <p class="watermark" aria-hidden="true">NOTES</p>
      <p class="kicker" data-reveal>Blog — Notes on AI Evaluation</p>
      <h1 data-reveal style="--d:0.08s">博客</h1>
      <p class="lead" data-reveal style="--d:0.16s">关于大模型评测、感知评测与仿真测试的实践笔记。</p>
    </header>

    <div class="post-list">
      {posts.map((p, i) => (
        <a class="post-row" href={`/blog/${p.id}/`} data-reveal style={`--d:${i * 0.06}s`}>
          <span class="post-date">{formatDate(p.data.date)}</span>
          <div>
            <h3 class="post-title">{p.data.title}</h3>
            <p class="post-desc">{p.data.description}</p>
            {p.data.tags.length > 0 && (
              <div class="post-tags">
                {p.data.tags.map((t) => (
                  <span class="tag">{t}</span>
                ))}
              </div>
            )}
          </div>
        </a>
      ))}
    </div>
  </div>
</Base>
